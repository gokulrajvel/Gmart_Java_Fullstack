package com.gokulrajvel.gmart.config;

// This session listener is no longer active because GMart has migrated to stateless JWT-based authentication.
// Standard HttpSessions are disabled, making session lifecycle tracking unnecessary.
public class SessionListener {
    // Disabled
}
